/**
 * Baseline Kit
 * Backwards-compatible entry point that re-exports from the new package structure.
 *
 * @module baseline-kit
 */

// Re-export everything from @baseline-kit/react
export * from '@baseline-kit/react'
